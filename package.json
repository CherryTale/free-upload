{
  "name": "free-upload",
  "displayName": "LazPay Efficient Tools",
  "icon": "images/upload_icon.jpg",
  "description": "An upload server.",
  "version": "0.0.18",
  "publisher": "zhuhaoran",
  "engines": {
    "vscode": "^1.86.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "free-upload.uploadStart",
        "title": "upload start"
      },
      {
        "command": "free-upload.uploadStop",
        "title": "upload stop"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Upload Configuration",
      "properties": {
        "free-upload.uploadFolder": {
          "type": "string",
          "default": "",
          "description": "The folder where uploaded files will be stored."
        },
        "free-upload.authToken": {
          "type": "string",
          "default": "",
          "description": "Token for the public server."
        }
      }
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test",
    "watch": "webpack --mode development --watch",
    "vscode:prepublish": "npm run package",
    "package": "webpack --mode production --devtool hidden-source-map && mkdir -p ./dist/fe && cp -r ./src/fe/.next ./dist/fe"
  },
  "devDependencies": {
    "@types/crypto-js": "^4.2.2",
    "@types/express": "^5.0.0",
    "@types/multer": "^1.4.12",
    "@types/qrcode-terminal": "^0.12.2",
    "dotenv": "^16.4.5",
    "eslint": "^8",
    "typescript": "^5",
    "webpack": "^5.95.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "@ngrok/ngrok": "^1.4.1",
    "crypto-js": "^4.2.0",
    "express": "^4.21.0",
    "multer": "1.4.5-lts.1",
    "next": "^14.2.15",
    "qrcode-terminal": "0.12.0",
    "socket.io": "4.7.5"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CherryTale/free-upload"
  }
}
